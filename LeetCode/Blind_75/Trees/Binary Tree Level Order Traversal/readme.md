## Binary Tree Level Order Traversal
```
use of queue and add null pointer after adding root in the q to determine about the completion of one level or we can create size variable inside the for loop and then iterate all over the queue until size get 0 the each time size get zero it show the completetion of one lever
```